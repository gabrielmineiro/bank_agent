[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bank-agent"
version = "0.1.0"
description = "Itaú Banking AI Agent"
authors = [{ name = "Gabriel" }]
dependencies = []

[tool.setuptools]
packages = ["agent", "kb", "mcp", "shared"]
